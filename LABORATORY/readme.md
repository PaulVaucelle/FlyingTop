To make it short, the ntuples are too lbig (in size), therefore MiniDATAMCNtupel  can not run at all and MiniNtuple can run  on all except TTTo2L2Nu and TTToSemiLeptonic ...
which makes this code with RDataFrame useless.

to run dataMCreader:

source DATAMCReader.sh